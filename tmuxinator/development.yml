# ~/.tmuxinator/development.yml

project_name: Development
project_root: ~/src/digalo-con-python
pre: export TERM=screen-256color-bce
tabs:
  - editor:
      layout: main-vertical
      panes:
        - vim
        - #empty, will just run plain bash
        - vagrant ssh
