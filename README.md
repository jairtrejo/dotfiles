Dotfiles
========

My configuration files for:

* git
* pentadactyl
* terminator
* tmux
* tmuxinator
* vim
* zsh

## Usage

Install and run `effuse`:

```
sudo gem install effuse -v 1.1.0
effuse --prefix="."
```
