[user]
  name = Jair Trejo
	email = jair@jairtrejo.mx
	signingkey = 36515C1E

[color]
  branch = auto
  diff = auto
  status = auto
	ui = auto

[color "branch"]
  current = yellow reverse
  local = yellow
  remote = green

[color "diff"]
  meta = yellow bold
  frag = magenta bold
  old = red bold
  new = green bold

[color "status"]
  added = green
  changed = yellow
  untracked = red

[github]
  user = jairtrejo

[alias]
  co = checkout
  st = status
  l = !zsh -c 'source ~/.githelpers && pretty_git_log'
	dag = log --graph --format='format:%C(yellow)%h%C(reset) %C(blue)%an <%ae>%C(reset) %C(magenta)%cr%C(reset)%C(auto)%d%C(reset)%n%s' --date-order
  pra = pull --rebase --autostash

[i18n]
  logoutputencoding = UTF-8
  commitencoding = UTF-8
[diff]
  tool = meld
[push]
	default = simple
[core]
	excludesfile = /Users/jair/.gitignore
	editor = nvim
	pager = less -FMRiX
